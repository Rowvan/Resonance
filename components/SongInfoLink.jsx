import { View, Text, Modal, TouchableOpacity } from 'react-native'
import React, { useState } from 'react'

const SongInfoLink = () => {

    const [showModal, setShowModal] = useState(false);

  return (
    <View>
      
    </View>
  )
}

export default SongInfoLink